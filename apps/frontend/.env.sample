# TaskWarrior-NG Frontend Environment Configuration

# API Host Configuration
# The host where the MCPO server is running
VITE_API_HOST=localhost

# API Port Configuration
# The port where the MCPO server is listening
VITE_API_PORT=8885

# API Protocol
# Use 'http' for local development, 'https' for production
VITE_API_PROTOCOL=http

# API Path Prefix
# The path prefix for all API endpoints
# MCPO appends the server name "taskwarrior" to the base prefix
# So with --path-prefix "/api/mcpo/", the full path is "/api/mcpo/taskwarrior/"
VITE_API_PREFIX=/api/mcpo/taskwarrior

# Full API URL (optional)
# If specified, this overrides the individual host/port/protocol settings
# VITE_API_URL=http://localhost:8885

# MCPO API Key
# Authentication key for MCPO server
VITE_MCPO_API_KEY=taskwarrior-secret-key

# Legacy Environment Variables (deprecated)
# These are kept for backward compatibility but should not be used
# VITE_MCPO_URL=http://localhost:8885